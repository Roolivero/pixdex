import { Href } from "expo-router";

export const ROUTES: Record<string,Href> = {
    HOME: "/",
    DETAIL: "/detail/",
    AHORCADO: "/ahorcado",
    PIXEL_REVEAL: "/pixel-reveal",
    GAME_OVER: "/game-over"
}